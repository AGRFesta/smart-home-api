providers:
  netatmo:
    base-url: https://netatmo.fake
    client-id: fake-client-id
    client-secret: fake-client-secret
    home-id: fake-home-id
  switchbot:
    base-url: https://switch-bot.fake
    token: fake-token
    secret: fake-secret
